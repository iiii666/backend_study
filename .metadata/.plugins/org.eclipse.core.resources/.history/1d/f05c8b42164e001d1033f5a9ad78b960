package codingtest;

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;

public class Main {
	public static void main(String[] args) throws NumberFormatException, IOException {
		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
		int num = Integer.parseInt(br.readLine());
		String[] arr = new String[num];
	
		for (int i = 0; i < num; i++) {
			arr[i] = br.readLine();
		}

	
		String temp = arr[0];
		String tempbox ="";
		for (int j = 1; j < arr.length; j++) {
			if (temp.length() > arr[j].length()) {
				tempbox = temp;
				arr[j] = arr[j + 1];
				arr[j + 1] = tempbox;
			} else if (arr[j].length() == arr[j + 1].length()) {
				StringBuffer sb = new StringBuffer(arr[j]);
				StringBuffer sb2 = new StringBuffer(arr[j + 1]);
				int temp2 = 0;
				while (temp2 < arr[j].length()) {
					if ((int) sb.charAt(temp2) > (int) sb2.charAt(temp2)) {
						tempbox = temp;
						arr[j] = arr[j + 1];
						arr[j + 1] = tempbox;
						break;
					}
					temp2++;
				}
			}
		}
		for(int i =0; i<arr.length;i++) {
			System.out.println(arr[i]);
		}
		

	}

}
